{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<table class="table table-striped">
			<thead>
				<th>nom</th>
				<th>Description</th>
			</thead>
			<tbody>
				{% for categorie in c %}
					<tr>
						<td>{{ categorie.namecateg }}</td>
						<td>{{ categorie.descriptioncateg }}</td>
                                                    <td>
                                <a href="{{ path('modifCateg',{'id':categorie.id}) }}">Modifier</a>
                            </td>
                            <td>
                                <a href="{{ path('supp_categ',{'id':categorie.id}) }}">Supprimer</a>
                            </td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
