{% extends 'base.html.twig' %}


{% block body %}

{{ include('_navbar.html.twig') }}
  <section class="breadcumb-area bg-img bg-overlay" style="background-image: url({{ asset ('img/bg-img/breadcumb3.jpg')}});">
        <div class="bradcumbContent">
            <h1>Select what suits you !</h1>
        </div>
    </section>
 
 <!-- ##### Events Area Start ##### -->
    <section class="events-area section-padding-100">
        <div class="container"> 
        {{ knp_pagination_render(offers,('paginator/pagination.html.twig')) }}      
            <div class="row">
            {% for ofs in offers %}

                <!-- Single Event Area -->
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="single-event-area mb-30">
                        <div class="event-thumbnail">
                            <img src="{{ asset('img/ofs-img/'~ofs.Image) }}" alt="">
                        </div>
                        <div class="event-text">
                        
                            <div class="event-meta-data">
                                <a href="#" class="event-place">Price</a>
                                <a href="#" class="event-date">${{ofs.price}}</a>
                            </div>
                          
                             <a href="{{path("selectedoffer", {'id': ofs.id})}}" class="btn see-more-btn">See infos</a>                             
                         
                        
                        </div>
                    </div>
                </div>
             {%endfor%}
            </div>
        </div>
    </section>
    <!-- ##### Events Area End ##### -->



{% endblock %}




